# Appwrite Configuration
VITE_APPWRITE_ENDPOINT=https://cloud.appwrite.io/v1
VITE_APPWRITE_PROJECT_ID=your_project_id_here
VITE_APPWRITE_DATABASE_ID=your_database_id_here
VITE_APPWRITE_COLLECTION_ID=your_collection_id_here

# Instructions:
# 1. Copy this file to .env
# 2. Replace the values with your actual Appwrite credentials
# 3. Create a project at https://cloud.appwrite.io
# 4. Create a database called "user_history"
# 5. Create a collection called "activities" with the following attributes:
#    - userId (string, required)
#    - action (string, required)
#    - data (string, required)
#    - timestamp (string, required)
# 6. Set collection permissions to allow authenticated users to create and read their own documents
